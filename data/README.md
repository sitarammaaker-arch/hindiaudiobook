# This folder stores dynamic audiobook data uploaded via the admin panel.
# audiobooks.json and books.json are created/updated at runtime.
# On Vercel: data persists within the same deployment but resets on redeploy.
# For permanent storage, consider upgrading to a real DB (Supabase/PlanetScale).
